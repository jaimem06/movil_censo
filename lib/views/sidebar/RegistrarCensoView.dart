import 'package:flutter/material.dart';

class RegistrarCensoView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registrar Censo'),
      ),
      body: Center(
        child: Text('Pantalla de registro de censo'),
      ),
    );
  }
}
