import 'package:flutter/material.dart';

class RegistrarCensadoView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registrar Censado'),
      ),
      body: Center(
        child: Text('Pantalla de registrar censado'),
      ),
    );
  }
}
